# Claude Code Guidelines - YF Portfolio to JSON

## プロジェクト概要
ヤフーファイナンス（日本版）のポートフォリオDOMから、銘柄コード、銘柄名、保有株数、取得単価、現在値等のデータを抽出し、構造化されたJSONに変換する。

## 技術スタック
- Chrome Extension Manifest V3
- TypeScript
- DOM Manipulation (Vanilla JS/TS)

## 命名規則
- キャメルケース（関数、変数）
- パスカルケース（クラス、型定義）

## 開発の優先事項
1. **堅牢なセレクタ定義**: Yahoo FinanceのDOM構造は変更される可能性があるため、セレクタは定数として管理し、エラーハンドリング（要素が見つからない場合の処理）を徹底する。
2. **クリーンなデータ構造**: 取得した数値データは、文字列（「1,000円」など）から純粋な数値（1000）へ変換した状態でJSON化する。
3. **拡張性**: 将来的には他媒体のインポート用フォーマットにも対応できるよう、変換ロジックを分離する。

## ビルド・テストコマンド
- ビルド: `npm run build`
- lint: `npm run lint`
